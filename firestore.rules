rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /routines/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /body/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
